http:
  routers:
    redirect-to-https:
      entryPoints:
        - web
      rule: "HostRegexp(`{host:.+}`)"
      middlewares:
        - redirect-to-https-302
      service: noop@internal

  middlewares:
    redirect-to-https-302:
      redirectScheme:
        scheme: https
        permanent: false
